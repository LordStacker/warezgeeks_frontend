export const getState = ({ setStore, getStore, getActions }) => {
    return {
        store: {
            Login_User: {
                email:"",
                password:""
            }
        },
        actions: {}
    }
}